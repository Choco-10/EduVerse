import React from "react";
import { Typography } from "@mui/material";

const Statistics = () => {
  return (
    <div>
      <Typography variant="h4" gutterBottom>
        Admin Dashboard
      </Typography>
      <Typography>
        Overview of users, classes, subjects, and activities will appear here.
      </Typography>
    </div>
  );
};

export default Statistics;
